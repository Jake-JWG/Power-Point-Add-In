# Assets folder

Binary icon files were removed to keep this repository fully text-only for Codex PR compatibility.

The add-in manifest now references hosted PNG icons from a public CDN.
If you prefer custom icons later, host PNG files externally and update the icon URLs in `manifest.xml`.
